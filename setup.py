from setuptools import setup

setup(
    name='ltipassparams',
    version='0.1',
    description='A JupyterLab extension to access LTI parameters',
    include_package_data=True,
    # data_files=[
    #     ("etc/jupyter/jupyter_notebook_config.d",
    #      ["jupyter-config/jupyter_notebook_config.d/ltipassparams.json"]),
    # ],
)